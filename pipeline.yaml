##Deprecated
kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: example-1
  namespace: jenkins
spec:
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5
  strategy:
    type: JenkinsPipeline
    jenkinsPipelineStrategy:
      jenkinsfilePath: Jenkinsfile
  source:
    type: Git
    git:
      uri: 'https://github.com/r3dl0ck/sample-helm-chart.git'
      ref: master
  triggers: []
  runPolicy: Serial
